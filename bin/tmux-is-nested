#!/usr/bin/env bash

set +e

_get_tmux_env_statu() {
  [ -z "$TMUX" ]
  local status="$?"
  echo $status
}

_echo_result() {
  if [ "$result" = "1" ]; then
    echo true
  else
    echo false
  fi
}

main() {
  local result=$(_get_tmux_env_statu)
  _echo_result
  exit 0
}

main
